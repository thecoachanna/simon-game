let buttonColors = ['red', 'green', 'blue', 'yellow']
let randomChosenColor = buttonColors[ran]
let gamePattern = []

function nextSequence() {
    let randomNumber = Math.floor(Math.random() * 4)
    alert(randomNumber)
}

